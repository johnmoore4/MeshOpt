#include "El3D.h"

using namespace std;

//void El3D::setChild(unsigned char ind, element_ptr el, 
//		    signed char orientation){
  //element2D_ptr el2d = static_pointer_cast<El2D>(el);
  //_children[ind] = el2d;
  //_child_orientation[ind] = orientation;
		      //}

/*
std::vector<node_ptr> El3D::projectPhysicalNodes(){
  
  arma::mat nodes0 = nodes();
  arma::mat map_matrix = _sf->sfmap.cols(_sf->interior_nodes);
  arma::mat xyz_new = nodes0*map_matrix;

  return createFreeNodes(xyz_new);
  
}
*/


//const bool El3D::is_curved() const{
 
  /*
  for(int i=0; i<_children.size(); i++){
    if(_children[i]->is_curved()) return true;
  }
  return _curved;
  */

//}

